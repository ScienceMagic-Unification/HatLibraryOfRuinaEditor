# 废墟图书馆 XML 编辑器 —— 通俗版完整开发计划

> 文档日期：2026-08-14
> 示例数据：`D:\Program Files (x86)\Steam\steamapps\common\Library Of Ruina\LibraryOfRuina_Data\Mods\梦幻宇宙\Data\CardInfo.xml`

---

## 一、这个工具要解决什么（大白话）

游戏 Mod 里那一堆 XML，本质就是一张张“数据表格”。现在只能拿记事本手改，容易写错标签、找不到对应关系、重复劳动。

我们要做的工具，就是给这些 XML 做一套“漂亮的图形化表格管理软件”：

- 不用记 XML 标签，点按钮、选下拉框就能改数据；
- 改完自动检查错误（ID 重复、枚举写错、引用的图片/脚本不存在）；
- 每次保存自动备份，写坏也能一键还原；
- 中文、英文翻译分开放，但点一条数据时自动对应显示；
- 每种 XML 一个专属工作区，互不干扰，以后新增 XML 类型像“批发”一样快。

---

## 二、整体思路（用一个比喻）

把**每种 XML 当成一个“台账”**，台账里一行就是一条记录（例如一张卡）：

| 区域 | 是什么 | 比喻 |
|---|---|---|
| 数据区 | XML 里的实体记录总表（卡号、稀有度、费用、骰子……） | 台账主表 |
| 本地化区 | 翻译表（中文名、英文名、描述） | 翻译对照表 |
| 编辑区 | 选中一条记录后，详细填写的表单 | 详情单 |
| 预览区 | 卡图 / 效果等可视化预览 | 预览窗 |
| 自动关联 | 表与表之间用“编号(ID)”连线 | Excel 的 VLOOKUP |

例如：在数据区点中卡 `ID=100 次元裂隙`，本地化区会自动跳到 `Localize\cn\BattlesCards` 里 `ID=100` 的那一条，预览区同时显示 `DreamUniverse_WCorp_TokenCard.png` 的卡图。改完一处的名字，另一处会提示是否同步。

---

## 三、用 Visual Studio 开发会怎么样

Visual Studio 有两种，先分清楚：

### 方案 A：VS Code + Node.js（Electron + React）—— 推荐
- 用“网页技术”做桌面软件（和做网站差不多）；
- 现成的漂亮 UI 组件极多（表格、下拉框、主题、动画），界面容易做得好看又统一；
- 组件天然模块化，很容易复用；
- 以后如果想出网页版，代码基本能搬过去。
- 缺点：安装包大一些（约 100MB）、需要学 JavaScript/TypeScript。

### 方案 B：Visual Studio 2022 + C#（WPF / WinUI）—— 备选
- 微软全家桶，做出正宗的 Windows 原生程序；
- 启动快、体积小，打包成一个 exe；
- XML 可以用 C# 自带的 XDocument 处理，正则用 .NET 正则，能力不差。
- 缺点：要做“美观统一 + 可复用组件库”，需要手写更多 XAML；现成的精致 UI 组件比网页生态少；整体开发速度慢，后续“批发新增 XML 类型”的模块化成本更高。

### 结论
这个项目的重点是**美观 + 模块化 + 快速新增**，所以推荐 **方案 A（VS Code + Electron + React）**。

你们 Mod 本体用 C# 写 DLL，和编辑器用什么语言写**完全互不影响**——编辑器只管读写 XML 文件。如果你团队更熟 C#，方案 B 也能做，但预计要多花 1.5~2 倍工时才能达到同等的界面效果。

---

## 四、核心设计（覆盖你提出的新需求）

### 4.1 每种 XML = 一个独立“工作区”
- 每个 XML 类型在应用里是一个**标签页工作区**，内容互不串扰；
- 工作区内分四块：**数据区、本地化区、编辑区、预览区（部分类型没有预览）**；
- 布局可以拖拽、保存自己的布局；
- “数据区 ↔ 本地化区”双向联动，靠 **ID 自动连线**；找不到对应记录时高亮警告；
- 每种 XML 可配置“要不要本地化区 / 要不要预览区”。

### 4.2 编辑区按 XML 类型定制
不用为每种 XML 单独写一整套界面，而是用**一份配置文件**描述它的字段，编辑器自动生成表单：
- 文本框、数字、下拉框、多选、开关；
- 骰子表格编辑器（BehaviourList）；
- 图片/脚本资源选择器（自动列出现有资源并显示缩略图）。

### 4.3 专属正则替换（按 XML 生效）
- 每个工作区可以带一套**自己的正则规则库**，规则不串区；
- 每条规则：名称、说明、作用范围（整个文件 / 选中记录 / 指定字段 / 仅本地化区 / 仅数据区）、查找式、替换式；
- 执行前**先预览命中数量和位置**，支持“逐条确认”或“一键全部替换”；
- 替换可撤销；替换后自动做 XML 合法性检查，防止把文件改坏。

### 4.4 “快速批发”新增 XML 类型（不影响旧内容）
采用**模块注册表（插件）机制**：每种 XML 类型 = 一个独立文件夹，里面只放配置：

```
modules/
  cardinfo/      schema.json 数据区结构 + localize.json 本地化关联
                 preview.tsx 可选预览 + rules.json 可选正则 + meta.json 图标/名称
  passive/       （新增时照抄上述结构）
  enemy/         （新增时照抄上述结构）
```

- 程序启动时自动扫描并加载这些文件夹；
- **新增一种 XML = 新建一个文件夹 + 填几份配置**，绝大多数情况不用改任何旧代码；
- 旧模块（如 CardInfo）零改动，天然互不影响；
- 只有遇到“需要特殊界面”的类型，才写少量扩展代码，原则是**只增不改**。

### 4.5 所有工作区共用的通用能力
撤销/重做、搜索过滤、批量修改、复制/删除/拖拽排序、ID 冲突检查、自动备份 + 原子保存、深色/浅色主题、中英界面。

---

## 五、分阶段开发计划

| 阶段 | 内容（大白话） | 交付验收标准 |
|---|---|---|
| **阶段 0：地基** | 搭好项目骨架：应用窗口、左侧模块导航、标签页工作区框架、统一配色和基础组件（按钮/表格/表单）、设置页（自动找游戏路径、选 Mod） | 打开应用，能看到一个好看、统一的空壳界面 |
| **阶段 1：核心引擎** | XML 保真读写（保留注释、格式、命名空间）、备份 + 原子保存、撤销/重做、校验引擎、带预览的正则引擎 | 改一个值并保存：注释和格式不变，游戏能正常读取 |
| **阶段 2：CardInfo 完整样板** | 数据区表格（118 张卡）、编辑区表单 + 骰子编辑器、本地化区联动（cn/en BattlesCards）、预览区卡图、专属正则示例、批量操作 | 对真实的“梦幻宇宙”CardInfo + 本地化全流程编辑并保存无误 |
| **阶段 3：批发机制验证** | 用“只写配置”的方式新增 Passive、Enemy 两个模块，验证不影响 CardInfo | 三个工作区标签页互相独立、互不干扰 |
| **阶段 4：批量补齐 + 进阶** | 补齐其余 XML（Stage / Book / DropTable 等）、跨文件引用检查、本地化同步、模板快速建卡、正则库 | 目录下所有 XML 都能用同一套编辑器工作 |
| **阶段 5：测试与打磨** | 单元测试 + Playwright 自动点击测试 + 截图视觉走查 + 大文件性能优化 | 测试通过，界面风格统一、不卡顿 |
| **阶段 6：发布** | 安全评审 + electron-builder 打包成 exe + 使用说明 | 产出可直接分发的安装包 |

---

## 六、建议先做什么（优先级）

1. **先做“地基 + 核心引擎”（阶段 0–1）**：没有底座，后面全是空中楼阁；
2. **再做 CardInfo 一个完整样板（阶段 2）**：一次性把“数据区 + 本地化区 + 预览区 + 专属正则”整条链路打通；
3. **立刻验证批发机制（阶段 3）**：用新增 Passive/Enemy 来证明“以后加新类型很快、旧的不受影响”；
4. **再批量补齐其余 XML（阶段 4）**；
5. **最后测试打磨（阶段 5）和打包（阶段 6）**。

原则：**每一步都交付一个“能打开、能用的版本”**，不要一次性做完才看效果。

---

## 七、任务安排在同一个窗口吗

**建议：同一个窗口，内部用“多标签页 + 分栏”组织。**

- 窗口顶部或左侧切换不同 XML 工作区（标签页）；
- 一个工作区内，数据区 / 本地化区 / 预览区左右分栏显示；
- 好处：联动同步最顺畅、内存占用小、切换快、整体风格统一；
- 当需要同时对照两个 XML 时，再提供“弹出副窗口”按钮（后期增强），**不要默认多窗口**；
- 本地化区做成“跟随选中行”的侧栏，需要大规模编辑时可放大成独立面板。

---

## 八、风险与注意事项

1. **文件安全**：保存采用“先写临时文件 → 校验合法 → 原子替换 + 自动 .bak 备份”，绝不直接覆盖；
2. **格式保真**：默认保留原文件缩进、注释、属性顺序；
3. **ID 冲突**：保存前自动检测，可自动分配下一个可用 ID；
4. **大文件性能**：上千条记录时使用虚拟滚动 + 防抖校验；
5. **杀软/权限**：游戏目录可能被 360 等拦截写入，保存失败要给出明确提示；
6. **已装开发技能**：`playwright`（UI 自动化测试）、`screenshot`（截图走查）、`security-best-practices`（发布前安全评审）。

---

## 九、建议的下一步

回复“开始”后，我先执行 **阶段 0 + 阶段 1**：搭建项目骨架与统一设计系统，并实现 XML 保真读写、备份、撤销/重做和校验引擎，让你第一时间能看到一个能打开、能编辑、能安全保存的真实窗口。

---

## 十、当前进度（2026-08-14 更新）

| 阶段 | 状态 |
|---|---|
| 阶段 0：应用骨架 + 统一设计系统 | ✅ 已完成 |
| 阶段 1：XML 核心引擎（保真读写/备份/撤销/校验/正则） | ✅ 已完成，11 项单元测试通过 |
| 阶段 2：CardInfo 完整样板（数据/本地化/预览/正则） | ✅ 已完成，真实数据验收通过 |
| 阶段 3：模块批发机制（Passive、Enemy 仅配置接入） | ✅ 已实测：Passive 直接可用、校验发现 8 个问题 |
| 阶段 4：补齐其余 XML 与高级功能 | ⏳ 待开始 |
| 阶段 5：Playwright UI 测试与视觉验收 | ⏳ 待开始（已装 playwright / screenshot 技能） |
| 阶段 6：安全评审与打包 | ⏳ 待开始（已装 security-best-practices 技能） |

运行方式见 README.md；界面截图见 docs/preview/prototype.png。